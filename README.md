# Shangban

一个 web 版本的哄自己上班软件。它用立体三花猫、翻页倒计时和可交互猫叫，帮你更温和地度过工作时间。

## 功能

- Three.js 立体三花猫
- 呼吸、点头、摇尾等实时动画
- 点击小猫触发不同猫叫声
- 上班前、上班中、下班后的翻页倒计时
- 本地读取上下班时间设置
- Netlify 静态部署配置

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

Netlify 构建命令为 `npm run build`，发布目录为 `dist`。
